toolsets = gnu vs2010;

exe hello {
    sources { hello.c }
    defines = PRINT_DETAILS;
    if ($(toolset) == gnu)
        defines += PLATFORM_UNIX;
    if ($(toolset) == vs2010)
        defines += PLATFORM_WINDOWS;
}
